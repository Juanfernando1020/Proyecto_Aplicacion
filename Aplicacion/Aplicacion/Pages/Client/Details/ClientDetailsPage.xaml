<?xml version="1.0" encoding="utf-8" ?>
<local:ContentPageBase
    x:Class="Aplicacion.Pages.Client.Details.ClientDetailsPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:extensions="clr-namespace:Xamarin.CommonToolkit.Markup.Extensions;assembly=Xamarin.CommonToolkit"
    xmlns:local="clr-namespace:Xamarin.CommonToolkit.PagesBase;assembly=Xamarin.CommonToolkit"
    xmlns:routes="clr-namespace:Aplicacion.Config.Routes;assembly=Aplicacion"
    xmlns:viewModel="clr-namespace:Aplicacion.Pages.Client.Details.ViewModel"
    Title="{Binding Client.Name}"
    x:DataType="{x:Type viewModel:ClientDetails}"
    BackgroundColor="{StaticResource MainBackground}">

    <local:ContentPageBase.Content>
        <StackLayout Padding="20">
            <ScrollView>
                <StackLayout Spacing="20">
                    <Button Command="{Binding CallClientCommand}" Text="Llamar al cliente" />
                    <Button Command="{Binding DelateClientCommand}" Text="Eliminar cliente" />
                    <Label Style="{StaticResource BoldLabel}" Text="Prestamos" />
                    <extensions:ViewsExtension View="{x:Static routes:ViewsRoutes.LoanList}" />
                </StackLayout>
            </ScrollView>
        </StackLayout>
    </local:ContentPageBase.Content>
</local:ContentPageBase>
